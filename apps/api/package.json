{
  "name": "@otunlink/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@otunlink/db": "workspace:*",
    "@otunlink/shared": "workspace:*",
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "~7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
