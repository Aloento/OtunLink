name = "otunlink-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# ── 占位绑定（ck-01 起逐个启用；在 CF 控制台创建后用 CLI 填入真实 id）──────
# [[hyperdrive]]
# binding = "HYPERDRIVE"
# id = "<hyperdrive-id>"

# [[kv_namespaces]]
# binding = "JWKS_CACHE"
# id = "<kv-namespace-id>"

# [[r2_buckets]]
# binding = "IMAGES"
# bucket_name = "otunlink-images"

# ── 环境变量（生产经 CF 控制台配置；本地开发放 .dev.vars）─────────────────
# [vars]
# DATABASE_URL = ""
